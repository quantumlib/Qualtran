---
trigger: glob
globs: "*.py"
description: "Enforce Google-style docstrings formatted in Markdown."
---

# Python Docstring Guidelines

* All Python docstrings must follow Google style conventions.
* Format docstrings using Markdown rather than reStructuredText (RST).
